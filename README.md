# Huffman-Compression
A simple implementation of Huffman's compression algorithm



Compile the Huffman.cpp file. Use the -c argument to compress and the -u argument to decompress. Input file as required.
